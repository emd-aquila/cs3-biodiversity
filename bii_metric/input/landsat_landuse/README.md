# Landsat land-use input

Create `landsat_landuse_long.csv` here using the schema documented in
`../../README.md`. Use a pre-resolved source class for forest state, for example
`primary_minimal`, `primary_other`, `secondary`, or `plantation`; do not map a
generic tree-cover class directly to primary vegetation.
